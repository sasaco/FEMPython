{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": "g++ -g ./src/main.cpp -o ./bin/main.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}